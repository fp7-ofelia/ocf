{% load ch_extras %}

<submit>
  <url>{{ url slice_flash_detail }}</url>
</submit>

{% for node in nodes %}
<node>
  <id>{{ node.nodeId }}</id>
  <x>{{ node.x }}</x>
  <y>{{ node.y }}</y>
  <sel_img>{{ node.sel_img }}</sel_img>
  <unsel_img>{{ node.unsel_img }}</unsel_img>
  <err_img>{{ node.err_img }}</err_img>
  <name>{{ node.nodeId }}</name>
  <is_selected>{{ node|is_in_set:slice.nodes.all }}</is_selected>
  <has_err>False</has_err>
</node>
{% endfor %}

{% for link in links %}
<link>
  <id>link.id</id>
  <src>
    <node_id>{{ link.src.ownerNode.nodeId }}</node_id>
  </src>
  <dst>
    <node_id>{{ link.dst.ownerNode.nodeId }}</node_id>
  </dst>
  <is_selected>{{ link|is_in_set:slice.links.all }}</is_selected>
  <has_err>False</has_err>
</link>
{% endfor %}
