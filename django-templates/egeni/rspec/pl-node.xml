{% load ch_extras %}
				<NodeSpec name="{{ node.name }}" type=""
				init_params=""
				cpu_min=""
				cpu_share=""
				cpu_pct=""
				disk_max=""
				start_time=""
				duration="">
					{% if node.interface_set.objects.all %}
					<net_if>
						{% for iface in node.interface_set.objects.all %}
						{% if slice|has_interface:iface %}
{% include "rspec/pl-interface.xml" %}
						{% endif %}
						{% endfor %}
					</net_if>
					{% endif %}
				</NodeSpec>