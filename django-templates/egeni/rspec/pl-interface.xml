					<IfSpec
					name="{{ iface.name }}" 
					addr="{{ iface.address }}"
					type="{{ iface.type }}"
					init_params="{{ iface.init_params }}"
					ip_spoof="{{ iface.ip_spoof }}"
					max_rate="{{ iface.max_rate }}"
					min_rate="{{ iface.min_rate }}">
					</IfSpec>