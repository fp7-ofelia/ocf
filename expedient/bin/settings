#!/bin/bash 

###
#	@author: msune
#	@organization: i2CAT
#	@project: Ofelia FP7
#       @description:settings file 
###

VERSIONS_PATH="$EXECPATH/versions/"

#Version files
CURRENT_VERSION_PATH="$EXECPATH/../.currentVersion" # Make sure this file is under .svnignore .gitignore... 
TARGETED_VERSION_PATH="$EXECPATH/../version" # Make sure this file is NOT under .svnignore .gitignore... 

#Use ofver in branch or out of the branch (either in another repository branch or in a separate repository)
OFVER_IN_BRANCH=1
